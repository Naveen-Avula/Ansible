- name: install and remove packages
  hosts: web
  tasks:
  - name: packages in loops
    ansible.builtin.dnf:
      name: "{{item.name}}"
      stae: "{{item.state}}"
    loop:
    - { name: 'mysql', state: 'latest'} # this is consider as item.
    - {name: 'nginx', state: 'absent'}
    

# latest means installing
# absent means uninstalling